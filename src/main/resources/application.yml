server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://shared-postgres:5432/userdb
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
